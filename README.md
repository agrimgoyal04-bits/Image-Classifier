# Image-Classifier
A deep-learning classifier built using lightweight Vision Transformers. The system is trained on a curated dataset of 10 everyday object categories and evaluated across two datasets to study generalization. Includes training analysis, inference scripts, and exported model weights.
